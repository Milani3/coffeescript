{
  "name": "coffeescript",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build --workspace=frontend",
    "dev": "npm run dev --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
